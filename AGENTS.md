# AGENTS.md — CMO War Room Dashboard

## 목적과 경계

이 저장소는 Virtual CMO의 정적 HTML 배포 결과물입니다. 생성·분석 로직의 기준은 `C:\Users\Lewis Taewan Kim\lewis-pictures-automation` 저장소이며, 여기서는 `index.html`과 날짜별 보고서만 관리합니다.

## 하드룰

- 한국어로 작업하고, 확인되지 않은 시장 사실·사람·회사·날짜를 추측하지 않습니다.
- 고유명은 **쏠레어**, **독안에 여자들**, **러브게임**, **Shadow Machine** 표기를 사용합니다.
- 이 저장소의 모든 파일은 외부에 게시될 수 있다고 가정합니다.
- API 키, 토큰, `.env`, 프로필, 내부 보고서 원문, 개인·재무 정보를 커밋하지 않습니다.
- `index.html`이 받는 API 키는 브라우저 로컬 저장소에만 저장되어야 하며 코드에 하드코딩하지 않습니다.
- 날짜별 보고서는 역사 스냅샷입니다. 과거 결과를 무단 개정하지 말고, 오류 원인을 생성기에서 고친 뒤 새 보고서로 발행합니다.

## 작업 흐름

1. 자동화 저장소의 CMO 생성기와 설정을 수정합니다.
2. 생성기 자체 검증과 시크릿 검사를 통과시킵니다.
3. 새 `report_YYYY-MM-DD.html`을 생성합니다.
4. `index.html`에서 최신 보고서 연결이 맞는지 확인합니다.
5. HTML에 키·토큰·로컬 경로·비공개 데이터가 없는지 검색합니다.
6. Lewis가 요청한 범위에서만 커밋·배포합니다.

## 이전 맥락

상세 운영 이력은 로컬 Claude 메모리의 Virtual CMO 관련 문서와 자동화 저장소의 `docs/handover/WORKSTREAMS.md`를 먼저 봅니다. 민감한 로컬 메모리를 이 저장소로 복사하지 않습니다.
